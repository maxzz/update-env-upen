{
  "name": "upen",
  "version": "1.0.0",
  "description": "CLI utility to update .env files with current date and build numbers",
  "main": "dist/main.js",
  "bin": {
    "update-env": "dist/main.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "dev": "ts-node src/main.ts",
    "prepublishOnly": "pnpm build",
    "deploy": "pnpm publish"
  },
  "keywords": ["cli", "env", "update"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}
